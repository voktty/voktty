Trabaja en /workspace/voktty y continúa el plan de optimización de cargas.

Antes de cambiar código, sigue AGENTS.md:
1. Lee VOKTTY.md completo.
2. Lee PROMPTS/iniciar.txt, PROMPTS/INDICE.md y PROMPTS/planes/PLAN_OPTIMIZACION_CARGAS_INTEGRACION.md.
3. Ejecuta git status --short.
4. Revisa el estado y las decisiones registradas en el plan.

Contexto de progreso:
- Último commit: e431f00 perf(startup): defer terminal copilot
- CP0 está completado.
- CP1 sigue abierto.
- Ya se difirieron API Client, Agent History, preloads oportunistas, Terminal Copilot y AgentRunBridge sin sesión activa.
- Métrica actual generada: index.html 175 recursos, 6,19 MiB raw, 1,74 MiB gzip; settings.html 44 recursos, 3,77 MiB raw, 1,05 MiB gzip.
- pnpm check-types, pnpm build y pnpm check:startup pasaron en el último cambio.
- pnpm lint tiene más de mil avisos preexistentes fuera de esta tarea; no realizar limpieza ajena.
- Cargo no está instalado, por lo que no se pueden ejecutar las validaciones Rust requeridas.

Problema prioritario pendiente:
dist/index.html y dist/settings.html todavía precargan ai-sdk-shared y ai-anthropic, aunque no debería existir un consumidor inicial de proveedor IA. Una prueba de separar ai, @ai-sdk/react y providers mediante manualChunks aumentó el arranque a 176 recursos y fue revertida. No repitas esa modificación sin evidencia del grafo.

Objetivo de la sesión:
Investiga con graphify y el bundle generado la relación exacta que hace que ai-sdk-shared dependa o precargue ai-anthropic. Propón e implementa solo una frontera pequeña, comprobable y reversible que reduzca carga inicial sin romper el compositor, Harness, Ajustes ni la primera apertura de IA.

Reglas:
- Usa graphify query, graphify explain y graphify path cuando estén disponibles; usa rg como respaldo.
- Inspecciona consumidores, tests y rutas de plataformas antes de editar contratos.
- No modifiques manualChunks a ciegas.
- Usa apply_patch.
- No incluyas refactors o limpieza no relacionada.
- Actualiza el plan en PROMPTS con evidencia, métricas, límites y siguiente paso.
- Antes de commit: git diff, git diff --check y git status --short.
- Ejecuta al menos pnpm check-types, pnpm build y pnpm check:startup; pruebas focalizadas según el cambio.
- No hagas commit salvo que el usuario lo pida explícitamente.
