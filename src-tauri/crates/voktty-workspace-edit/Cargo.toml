[package]
name = "voktty-workspace-edit"
version.workspace = true
edition.workspace = true
license.workspace = true
publish = false

[dependencies]
hex = "0.4"
regex = "1"
serde = { version = "1", features = ["derive"] }
sha2 = "0.10"

